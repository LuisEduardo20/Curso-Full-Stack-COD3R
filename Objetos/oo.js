// CÓDIGO NÃO EXECUTÁVEL

// Procedural
processamento(valor1, valor2, valor3)

// OO
objeto = {
  valor1,
  valor2,
  valor3,
  processamento() {
    // ...
  }
}

objeto.processamento() // Foco passou a ser o objeto

// Principios importantes:
// 1. abstracao -> Abstrair informações do mundo real que sejam relevantes para o seu software
// 2. encapsulamento -> expor somente pequenas partes do todo para o mundo
// 3. herança (prototype) -> relações entre objetos (E1) onde existem objetos pais que tem filhos com características herdadas
// 4. polimorfismo -> atibuir características dos filhos ao pai(mais genérico)
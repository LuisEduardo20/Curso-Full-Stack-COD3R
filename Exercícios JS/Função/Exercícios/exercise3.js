/**
 * 03)​ Crie uma função que recebe dois parâmetros, base e expoente, 
 * e retorne a base elevada ao expoente.
 */

function baseExp(base, exp) {
  return base ** exp
}

console.log(baseExp(2, 4))